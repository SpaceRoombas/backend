// Generic root to wrap all flatbuffers
// The reciving end will take "hint" and instantiate the mapped object
// we do this to avoid runtime reflection.

enum message_type:byte { Message = 0, ACK = 1, Handshake = 2}

table carrier_pigeon {
    type:message_type;
    hint:string;
    payload:[ubyte];
}

root_type carrier_pigeon;