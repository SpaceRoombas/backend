

table session_handshake {
    
    // scalars
    creation_time:uint32;

    // non-scalars
    username:string;
    signature:string; // In the future, that server front-end may sign these handshakes to avoid fake handshakes from being sent
}